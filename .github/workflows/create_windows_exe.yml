name: create_windows_exe
run-name: ${{ github.actor }} is Creating a windows version of Streamroller
on: [push]

jobs:
  create_windows-version:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '16.13.2'
      - run: |
            npm i -D caxa
            npx caxa -i . -o StreamRoller.exe -- "{{caxa}}/node_modules/.bin/node" -- "{{caxa}}/backend/data_center/server.js"
#      - uses: actions/upload-artifact@v3
#        with:
#          name: my-artifact
#          path: StreamRoller.exe
      - run: |
          git config --global user.email "silenusta.com"
          git config --global user.name "silenusta"
          git config --global credential.helper cache
          git clone https://${{secrets.ACCESS_TOKEN}}@github.com/SilenusTA/StreamRollerRelease sr_clone
          ls -la
          ls -la /sr
#                  cp index.js clonedFolder/ -f
#                  cd clonedFolder
#                  git add .
#                  git commit -m "$(date)"
#                  git push
#  push_file:
#    runs-on: ubuntu-latest
#    needs: create_windows-version
#    env:
#        API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }}
#    steps:
#      - uses: actions/checkout@v3
#      - run: git config http.postBuffer 524288000
#      - uses: actions/download-artifact@v3
#        with:
#          name: my-artifact
#      - name: Display structure of downloaded files
#        run: ls -R
#      - uses: dmnemec/copy_file_to_another_repo_action@main
#        name: push_to_repo
#        with:
#          source_file: 'StreamRoller.exe'
#          destination_repo: 'SilenusTA/StreamRollerRelease'
#          destination_folder: 'windows_releases'
#          user_email: 'silenusta@gmail.com'
#          user_name: 'silenusTA'
#          commit_message: 'Software Release'
#      - run: echo finished
      
